sonar.projectKey=devopsfoundry_tfl-journey-expense-calculator
sonar.organization=devopsfoundry
sonar.projectName=TfL Journey Expense Calculator
sonar.sourceEncoding=UTF-8

# Sources live at repo root (e.g., App.tsx, components/, services/, server/)
sonar.sources=.

# No dedicated test directory; adjust if tests are added later
# sonar.tests=tests
# sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

sonar.exclusions=**/node_modules/**,**/dist/**,**/coverage/**,**/.github/**,**/.husky/**,**/sample/**

# Add coverage report path if generated (e.g., via vitest/jest)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
